# Sponsorship channels for the URUS Foundation.
#
# Status note (per issue #168):
#   The original patreon entry (id "15684319") returned 404 — the
#   Patreon page never existed. Until the URUS Foundation has its
#   own organization-level sponsor accounts set up, **all funding
#   channels below temporarily point at the founder's personal
#   accounts** (Rasya Andrean). Donations are still in service of
#   the URUS project; we just don't have a separate foundation
#   wallet yet.
#
# When the foundation registers its own accounts these entries will
# move from personal to foundation handles. The URLs in the
# `custom:` block are pinned to the founder's profile pages, so a
# user visiting them today reaches an active sponsor page rather
# than a 404.

github: [RasyaAndrean]
ko_fi: rasyaandrean
custom:
  - 'https://saweria.co/rasyaandrean'
  - 'https://trakteer.id/rasya_andrean'
